{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx nixpacks build --node-version 20"
  },
  "deploy": {
    "startCommand": "npm run server",
    "healthcheckPath": "/api/health"
  },
  "nixpacks": {
    "node": "20"
  }
}
